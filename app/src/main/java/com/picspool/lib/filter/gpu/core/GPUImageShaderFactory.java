package com.picspool.lib.filter.gpu.core;

import com.picspool.lib.filter.gpu.GPUFilterType;

/* loaded from: classes3.dex */
public class GPUImageShaderFactory {
    public static String getVertexShader(String str) {
        if (str == GPUFilterType.VIGNETTE_SHARPNESS.toString()) {
        }
        return "";
    }

    public static String getFragmentShader(String str) {
        if (str == GPUFilterType.VIGNETTE_SHARPNESS.toString()) {
        }
        return "";
    }
}
